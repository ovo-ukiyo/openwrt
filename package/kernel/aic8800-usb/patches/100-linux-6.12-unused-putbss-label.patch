--- a/src/USB/driver_fw/drivers/aic8800/aic8800_fdrv/rwnx_msg_rx.c
+++ b/src/USB/driver_fw/drivers/aic8800/aic8800_fdrv/rwnx_msg_rx.c
@@ -825,6 +825,12 @@ static inline int rwnx_rx_scanu_result_i
 #endif
 
     }
+    /*
+     * CONFIG_USE_WIRELESS_EXT is disabled on OpenWrt.  Keep a syntactic
+     * reference to the shared cleanup label so GCC does not reject the
+     * vendor source with -Werror=unused-label.
+     */
+    if (0) goto putbss;
 putbss:
     if (bss != NULL)
 #if LINUX_VERSION_CODE < KERNEL_VERSION(3, 9, 0)
